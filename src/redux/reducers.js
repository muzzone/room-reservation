
export const popover = (state = [], action) => {
  switch (action.type) {
    case 'SHOW_POPOVER' :
      return state;
    case 'HIDE_POPOVER' :
      return state;
    default:
      return state;
  }
};
